<Page
    x:Class="Hel.App.WinUI.Pages.SelectLocationsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="{StaticResource ShellPadding}" Spacing="16">
            <Border
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Select Locations" FontSize="24" FontWeight="SemiBold" />
                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" TextWrapping="WrapWholeWords" />

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button Content="Select all" Click="SelectAllButton_Click" />
                        <Button Content="Clear all" Click="ClearAllButton_Click" />
                    </StackPanel>

                    <ItemsControl ItemsSource="{x:Bind ViewModel.Locations, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="models:LocationSelectionItemViewModel"
                                          xmlns:models="using:Hel.App.WinUI.Models">
                                <CheckBox
                                    Content="{x:Bind DisplayLabel, Mode=OneWay}"
                                    IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
