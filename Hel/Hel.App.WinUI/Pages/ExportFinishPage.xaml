<Page
    x:Class="Hel.App.WinUI.Pages.ExportFinishPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="{StaticResource ShellPadding}" Spacing="16">
            <Border
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Generate reports" FontSize="24" FontWeight="SemiBold" />
                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" TextWrapping="WrapWholeWords" />

                    <Button Content="Choose Output Folder" Click="ChooseOutputFolderButton_Click" HorizontalAlignment="Left" />

                    <TextBlock Text="Selected output folder" FontWeight="SemiBold" />
                    <TextBox Text="{x:Bind ViewModel.SelectedOutputFolder, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <InfoBar
                IsOpen="{x:Bind ViewModel.HasGeneratedFiles, Mode=OneWay}"
                IsClosable="False"
                Severity="Success"
                Title="Export complete"
                Message="{x:Bind ViewModel.SuccessMessage, Mode=OneWay}" />

            <Border
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Generated files" FontSize="18" FontWeight="SemiBold" />

                    <ItemsControl ItemsSource="{x:Bind ViewModel.GeneratedFileNames, Mode=OneWay}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="x:String">
                                <TextBlock Text="{x:Bind}" TextWrapping="WrapWholeWords" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button Content="Open Output Folder" Click="OpenOutputFolderButton_Click" />
                        <Button Content="Open Log Folder" Click="OpenLogFolderButton_Click" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
