<Page
    x:Class="Hel.App.WinUI.Pages.LoadCsvPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="{StaticResource ShellPadding}" Spacing="16">
            <Border
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
                <StackPanel Spacing="12">
                    <TextBlock Text="Load CSV" FontSize="24" FontWeight="SemiBold" />
                    <TextBlock Text="Select the Monthly Missing Items CSV file to begin the guided workflow." TextWrapping="WrapWholeWords" />

                    <Button Content="Select CSV" Click="SelectCsvButton_Click" HorizontalAlignment="Left" />

                    <TextBlock Text="Library scope" FontWeight="SemiBold" />
                    <TextBlock Text="{x:Bind ViewModel.LibraryScopeName, Mode=OneWay}" />

                    <TextBlock Text="Loaded CSV path" FontWeight="SemiBold" />
                    <TextBox Text="{x:Bind ViewModel.LoadedCsvPath, Mode=OneWay}" IsReadOnly="True" TextWrapping="Wrap" />

                    <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                </StackPanel>
            </Border>

            <Border
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource CardCornerRadius}"
                Padding="{StaticResource CardPadding}">
                <Grid ColumnSpacing="24" RowSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Records loaded" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.RecordCount, Mode=OneWay}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Available locations" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.LocationCount, Mode=OneWay}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Parse failures" FontWeight="SemiBold" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.ParseFailuresCount, Mode=OneWay}" />
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
