<Page
    x:Class="Hel.App.WinUI.Pages.StartPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="{StaticResource ShellPadding}">
        <Border
            Background="{StaticResource CardBackgroundBrush}"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource CardCornerRadius}"
            Padding="{StaticResource CardPadding}">
            <StackPanel Spacing="16">
                <TextBlock
                    Text="Welcome"
                    FontSize="28"
                    FontWeight="SemiBold" />

                <TextBlock
                    Text="William Allen White Library - Emporia State University"
                    Opacity="0.85"
                    FontSize="15" />

                <TextBlock
                    Text="{x:Bind ViewModel.WelcomeText, Mode=OneWay}"
                    TextWrapping="WrapWholeWords" />

                <TextBlock
                    Text="Workflow"
                    FontSize="18"
                    FontWeight="SemiBold" />

                <TextBlock Text="1. Load the monthly CSV export." />
                <TextBlock Text="2. Review and adjust location scope." />
                <TextBlock Text="3. Preview routing results, warnings, and data quality." />
                <TextBlock Text="4. Generate recipient-ready TXT files and run summary." />
            </StackPanel>
        </Border>
    </Grid>
</Page>
