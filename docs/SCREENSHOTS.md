# Screenshot guide

Add the current app screenshots to:

- `docs/screenshots/start.png`
- `docs/screenshots/load-csv.png`
- `docs/screenshots/select-locations.png`
- `docs/screenshots/preview-results.png`
- `docs/screenshots/export-finish.png`

## Capture notes

Use the current polished Phase 9 / Phase 9.1 UI.

### start.png
Capture the Start page with the sidebar visible.

### load-csv.png
Capture the Load CSV page with a loaded CSV path visible.

### select-locations.png
Capture the Select Locations page with the WAWL location checklist visible.

### preview-results.png
Capture the Preview Results page with:
- run summary totals
- recipient bucket preview
- warning bars if visible

### export-finish.png
Capture the Export & Finish page after successful export with:
- success confirmation
- generated file names
- output folder shown

## Recommendation

Use the screenshots you already captured during Phase 9 validation, then rename them to these exact filenames before committing.